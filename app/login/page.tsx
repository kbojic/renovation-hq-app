export default function LoginPage() {
  return (
    <main style={{ maxWidth: 520, margin: "40px auto", padding: 24 }}>
      <h1>Sign in</h1>
      <p>
        Login will connect here after Supabase Auth is fully wired. For now,
        deploy the app first and confirm the main dashboard loads.
      </p>
      <a href="/">Back to app</a>
    </main>
  );
}
